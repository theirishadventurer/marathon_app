# Session 2.7 — Cross-cutting review: Feat A / B / C / D

**Status:** review (no code, no spec edits)
**Date:** 2026-05-07
**Reviewer:** session lead (CBell)
**Branch:** `session-2/backend-move-endpoints`
**Inputs:**
- `docs/superpowers/specs/2026-05-07-feat-a-mark-complete-dialog-design.md` (commit `c6d80d4`)
- `docs/superpowers/specs/2026-05-07-feat-b-recent-runs-and-coach-brief-design.md` (commit `8fb8ee4`)
- `docs/superpowers/specs/2026-05-07-feat-c-program-start-date-design.md` (commit `a364fbd`)
- `docs/superpowers/specs/2026-05-07-feat-d-visual-crispness-design.md` (commit `8922eb7`)

---

## Executive summary

Yes, the four specs are implementable as one sprint, but **not in arbitrary order, and not without three small decisions from the user up front.** Feat D (typography) must land first or in lockstep with the touched components — if A/B/C ship with PressStart2P everywhere and D rewrites the same files a day later, every UI PR has to be reopened. Feat B introduces an umbrella `invalidate_for_athlete(athlete_id)` cache helper (Feat B `coach-brief.md:294`) that A and C both implicitly depend on; this helper is shared infrastructure and should land before A's and C's mutation endpoints. Feat A's `nullable garmin_activity_id` migration and Feat C's `plan_history` migration are independent and can ship in parallel. Feat A's `SYNC` pill on `TodayScreen` (Feat A §7) and Feat B's coach-brief + recent-runs replacement on the same screen (Feat B `feat-b §6 component plan`) both edit `mobile/src/screens/TodayScreen.tsx` — they must be consolidated into one `TodayScreen` PR or they'll merge-conflict. The biggest blocker is the **`abandoned` `WorkoutStatus`** open question (Feat C open Q2) — it touches the schema enum, Feat A's state guard (Feat A §4 step 2), and Feat D's WeekTile/status-pill rendering. If the user wants `abandoned`, the migration should land alongside Feat A's nullable migration in Phase 1 so all downstream code can rely on the enum.

**Top 3 blocking open questions for the user (must answer before code starts):**

1. **Add a new `abandoned` `WorkoutStatus` value?** (Feat C open Q2.) If yes, it's a schema migration in Phase 1; if no, Feat C reuses `skipped` and the dialog copy clarifies. This affects Feat A (state guards), Feat B (adherence math in `compose_today_brief`), and Feat D (status-pill color/copy in `WorkoutCard.tsx:48`).
2. **`MIN_WEEKS_TO_RACE` thresholds (16 / 8 / 9).** (Feat C open Q5.) These are guesses. Need confirmation from the coach before Feat C's validation logic locks in — they're a hard `400` boundary that affects whether the Settings → Program panel is even usable to the active athlete.
3. **Token names — `mono` / `monoBold` vs `body` / `bodyBold`.** (Feat D open Q2.) Trivial to decide, but blocks Feat D's `tokens.ts` PR which is the foundation for every other UI PR in the sprint. Must be decided before any A/B/C mobile work starts.

---

## 1. Per-spec quality check

### Feat A — Manual mark-complete + workout-data dialog

**Quality: high.** The spec is implementable as written. UX placement is justified (Feat A §2 picks "Detail-only" with reasoning), the bottom-sheet field plan mirrors `EditQuestSheet` patterns we already have, the backend endpoint is fully specced down to the SQLAlchemy lookup pattern, and the reconciler-interaction §5 is genuinely the right call (manual log wins; Garmin becomes a bonus — falls out of existing reconciler with zero changes).

**Internal contradictions:** none material. One small inconsistency: Feat A §6 says the sheet "will adopt Feat D polish in parallel without code changes" but §3 hard-codes `'PressStart2P' 8pt` and `'VT323' 18pt` in the field labels — once Feat D lands, the labels should be `mono 11` and inputs `VT323 18` (Feat D §4.10 EditQuestSheet row maps the equivalent labels). Implementer needs to know to use Feat D tokens, not literal font names.

**Unstated assumptions:** Feat A §4 step 5 sets `started_at = midnight on scheduled_date`. The spec acknowledges this is "honest" (open Q2) but doesn't note that `tests/` may need updating if any test asserts a non-midnight `started_at` for matched completions — search will find it. Also: §4.1 picks Option A (nullable `garmin_activity_id`) without noting that **Postgres unique constraints allow multiple NULLs by default**, which is exactly what we need — that's correct, but worth flagging that on **SQLite** (used in tests) the same is true so it's safe.

**Unimplementable bits:** none. The `LogCompletedRequest` schema and route handler are concrete enough to type out.

### Feat B — Recent runs strip + Coach brief

**Quality: high.** The two sub-features are cleanly separated, the `compose_today_brief` heuristic is concrete (Feat B §4 pseudo-Python), the cache invalidation story is the most thought-through of any of the four specs — it explicitly proposes the `invalidate_for_athlete` umbrella (Feat B §4 Caching → "central `invalidate_for_athlete(athlete_id)` helper that fans out").

**Internal contradictions:** the spec states no new DTOs are needed (Feat B §3 → "Reuses the existing schema in `app/schemas/workout.py:12-27` — no new DTO needed") but Feat A is simultaneously **changing that schema** to make `garmin_activity_id` nullable (Feat A §8 → `app/schemas/workout.py:17` → `garmin_activity_id: int | None`). Not a conflict in semantics — both specs leave the field shape compatible — but the mobile types regen happens once and Feat B's "no new types" claim is only true *after* Feat A's nullable change has shipped (otherwise `null` from a manual log would fail OpenAPI client validation if the generated type was still `number`).

**Unstated assumptions:** `_yesterday_completion` (Feat B §4) joins through `Reconciliation`, but the spec doesn't say what to do when an athlete has **multiple** matched completions for yesterday (e.g. ran in the morning, lifted in the afternoon, both planned and matched). The pseudocode treats it as a single row. Implementer should pick "highest match_confidence then most recent `started_at`" or document the choice.

`adh_total >= 3` (Feat B §4) — this means a brand-new athlete with only 1-2 days of plan history sees no adherence sentence. That's correct but not stated.

**Unimplementable bits:** none, but `tests/test_plan_routes.py:20` is called out (Feat B §8 Done criteria → "currently asserts `coach_brief is None`") and **will break** the moment the brief returns a string. Feat B owns the fix; the planner needs to schedule it inside Feat B's PR.

### Feat C — Start-date picker + auto-recalculate

**Quality: high, with the most open questions.** The α / β / γ tradeoff analysis (Feat C §3) is excellent. The `plan_history` model is well-designed (append-only ledger, separate from `plans` itself).

**Internal contradictions:** the spec proposes `POST /plan/start-date?dry_run=true` (Feat C §5) and *also* refers to a sibling endpoint `POST /plan/start-date/preview` in the UX placement section (Feat C §6). Same handler, two different URL patterns. Pick one — recommend the `?dry_run=true` query-param form because it matches existing FastAPI conventions and avoids a second route registration.

**Unstated assumptions:** Feat C §5 step 5 (validation) computes the active cycle as "Cycle.start_date <= today <= end_date, or earliest unfinished." That logic isn't in `app/services/plan_aggregator.py` today — `_active_cycle` exists but uses different criteria. Implementer needs to either reuse `_active_cycle` exactly or write a new helper and document why. Feat C §8 component plan claims "verify `_active_cycle` still picks the right cycle after a shift (it does — it queries by start_date <= today)" — should be verified, not assumed, in tests.

The migration question (Feat C §8 → "Project currently has no `migrations/` dir; either start one or extend `schema.sql`") is wrong — the project **does** have alembic (`alembic/versions/`) per Feat A §4.1. The reviewer should have checked. So Feat C's `plan_history` migration is just another alembic revision, not a new infrastructure decision.

**Unimplementable bits:** the `abandoned` status (open Q2) is unresolved. Feat C §5 step "Side effects" says "Mark `planned_workouts.status = 'skipped'`" — but the open question explicitly flags that "skipped implies user choice, which feels wrong." Whichever way the user decides, the spec needs the answer before the engineer can write the UPDATE statement.

### Feat D — Visual crispness

**Quality: high and exceptionally thorough.** The audit table (Feat D §2) classifies every PressStart2P site in the app with file:line refs and a verdict. The font choice (JetBrains Mono) is justified, the loading approach matches the existing `useFonts` pattern, the per-element table (Feat D §3.4) is implementer-ready.

**Internal contradictions:** Feat D §4.10 says EditQuestSheet `EDIT QUEST` title becomes "monoBold 18 accentHi, drop the `.toUpperCase()` (it's a verb-noun command)" — but §2 audit table labels the same line "**SWAP→monoBold** | monoBold 18". These are consistent; the §4.10 row adds the casing decision. Just flagging that the audit table is the canonical reference, with case decisions only in §4.

**Unstated assumptions:** the spec notes that JetBrains Mono Regular and Bold must be registered as **separate families on RN** because `fontWeight` is unreliable on Android (Feat D §3.2 note). Correct, but the implementation tip applies to *every* place we currently use `fontWeight: '700'` on a VT323 element (e.g. WhySheet `strong`, Feat D §4.9). Worth flagging that the bold-via-weight pattern is broken everywhere on Android, not just where Feat D explicitly swaps.

**Unimplementable bits:** none. The 10-step implementation outline (Feat D §8.4) is granular enough to PR row-by-row.

---

## 2. Cross-cutting conflicts

### 2a. Backend schema conflicts

- **Feat A's `nullable garmin_activity_id` migration** (Feat A §4.1) and **Feat C's `plan_history` migration** (Feat C §2 + §8) are **independent**. Different tables (`completed_workouts` vs new `plan_history`), no overlapping columns. Migration ordering: either order works; alembic linearizes by revision. Both can be authored in parallel and merged in any order.
- **No two features add the same column to the same table.**
- **Feat C's status mutation** (Feat C §5 → marks abandoned planned rows as `skipped`) does **not** conflict with Feat A's manual log path (which sets `planned.status = done`) because they operate on disjoint row sets (Feat C: rows in the past with status=planned; Feat A: a single row the user has selected). However, **if `abandoned` becomes a new enum value** (open Q2), the migration adds a new `WorkoutStatus` value and Feat A's state guard at §4 step 2 (`status in (done, skipped)`) becomes incomplete — should it 409 on `abandoned` too? Almost certainly yes (you can't "log completion" for a workout the system has marked abandoned), but Feat A doesn't currently handle it.

### 2b. API surface conflicts

No overlapping route paths. Endpoints proposed:

| Feature | New routes |
|---|---|
| Feat A | `POST /workouts/{id}/log-completed` |
| Feat B | `GET /workouts/completed/recent` |
| Feat C | `POST /plan/start-date` (with `?dry_run=true`) |
| Feat D | none (frontend only) |

`/workouts/completed/recent` and `/workouts/{id}/log-completed` both live in `app/routes/workouts.py`. FastAPI route registration order matters when paths could collide — `/workouts/completed/recent` must be registered **before** any `/workouts/{path-param}` route, otherwise `completed` gets parsed as a workout id. Existing `GET /workouts/{id}` at the file does this correctly today; the implementer must place Feat B's new handler above it. Worth a unit test (`GET /workouts/completed/recent` returns 200, not 404 from id-parse).

**Response-shape conflict:** Feat A's `LogCompletedResponse` returns `{planned, completed, reconciliation}` (Feat A §4 Response). Feat B's recent-completed endpoint returns `list[CompletedWorkoutOut]`. Both reuse the same `CompletedWorkoutOut`. After Feat A makes `garmin_activity_id` nullable, Feat B's response gets `garmin_activity_id: int | None` — this is a **wire-format change** for any existing mobile code that already consumes `CompletedWorkoutOut`. The OpenAPI regen catches it; the planner must ensure the mobile types are regenerated **after** Feat A's schema change lands and **before** Feat B's mobile hook is written, or there'll be a brief window where the generated type is wrong.

### 2c. Mobile component conflicts

- **`mobile/src/screens/TodayScreen.tsx` is touched by Feat A, Feat B, AND Feat D.**
  - Feat A §8 modifies it to add a header `SYNC` pill (Feat A §7).
  - Feat B §6 modifies lines 78-85 (coach-brief placeholder) and 119-126 (recent-runs placeholder).
  - Feat D §4.14 modifies the page-title typography on the same screen.
  - **Recommendation:** consolidate all three into one `TodayScreen restyle + wire-up` PR sequenced after the foundational pieces (Feat D tokens, Feat B endpoint, Feat A endpoint). Or split into "Feat D typography on TodayScreen" first, then "Feat A SYNC pill + Feat B brief/strip" in one PR — this is the cleaner split because Feat D's text-only edits are easy to merge.

- **`mobile/src/components/EditQuestSheet.tsx`** — Feat A's `LogCompletedSheet` is modeled on it (Feat A §3 → "mirrors `EditQuestSheet`") and Feat D rewrites EditQuestSheet's typography in §4.10. If the engineer copies EditQuestSheet's font usage into `LogCompletedSheet` *before* Feat D lands, they'll copy the old `'PressStart2P' 8pt` literals; if after, they get the new tokens. **Strong argument for D before A on the mobile side.**

- **`mobile/src/screens/SettingsScreen.tsx`** — Feat C §6 adds a Program section. Feat D §4.14 changes the page title to monoBold 20 mixed-case. No conflict on the panel itself, but the Program block must use the new font tokens. Same D-before-C argument.

- **`mobile/src/components/WorkoutCard.tsx`** — Feat A explicitly does NOT touch it (Feat A §8 → "Untouched"); Feat D does (Feat D §4.4). No conflict.

- **`mobile/src/components/program/WeekTile.tsx`** — Feat D §4.6 leaves it unchanged. Today the WeekTile renders a status-driven mileage glyph. **If `abandoned` becomes a new status, WeekTile's status-color switch may need a new branch** — Feat D didn't anticipate this because it didn't know about Feat C's open Q2.

### 2d. Cache invalidation overlap

This is the **most material cross-cutting concern in the sprint.**

Caches in play:
- `_PLAN_FULL_CACHE` and `_PLAN_STATS_CACHE` in `app/services/plan_aggregator.py` (existing).
- New: `_RECENT_COMPLETED_CACHE` in Feat B (Feat B §3 Caching).
- New: coach-brief cache in `app/services/coach_brief.py` (Feat B §4 Caching).

Mutation points and invalidations needed:
| Mutation | plan caches | recent-completed | coach-brief |
|---|---|---|---|
| Feat A `log-completed` | yes | **yes** | **yes** (today's brief depends on yesterday's completion) |
| Feat B (no mutations) | — | — | — |
| Feat C `start-date` | yes | no (raw completions don't move) | **yes** (today changes) |
| Garmin sync (existing) | yes | **yes** | **yes** |
| Feat A skip / edit / move (existing) | yes | no | **yes** (status change moves adherence) |

Feat B proposes the umbrella `invalidate_for_athlete(athlete_id)` (Feat B §4 Caching). **Feat A does NOT mention it** — Feat A §4 step 8 says only `invalidate_plan_cache(athlete.id)`. **Feat C does NOT mention it** — Feat C §5 says only `invalidate_plan_cache`.

If Feat B's umbrella lands and A/C don't adopt it:
- Feat A's manual log busts plan_full and plan_stats but **leaves the recent-completed cache and coach-brief cache stale for up to 60s.** User logs a run, opens TodayScreen, sees yesterday's strip; pulls to refresh, still stale. Bad.
- Feat C's start-date reset busts plan caches but **leaves coach-brief stale.** User shifts the plan, sees yesterday's coach brief talking about a workout that's now next week. Bad.

**Mandate:** the umbrella `invalidate_for_athlete` should land **first** (Phase 1 of the sprint), and Feat A and Feat C must call it instead of `invalidate_plan_cache`. The existing `invalidate_plan_cache` becomes a private helper inside the umbrella.

### 2e. Test fixture conflicts

- **`tests/test_plan_routes.py:20`** — Feat B explicitly calls this out (Feat B §8 → "currently asserts `coach_brief is None`"). The fixture flips: with brief now populated, the test must be reworked to either set up an athlete with no plan/no completions (asserts None) or assert string content. **Feat B owns this fix in its own PR.**
- **No existing reconciler test will break from Feat A.** The reconciler at `app/services/reconciler.py:60` filters candidates by `status.in_([planned, moved])`, so manually-logged `done` rows are excluded. The bonus-run path (Feat A §5) is covered by existing logic. Feat A §8 adds a *new* integration test for the manual-log + later-Garmin-sync interaction; it's additive, not a fix.
- **Feat C's date shift will break any test that hardcodes seed dates** — search for `2026-04-13` (the seeded Phase 1 start, per Feat C §1) and `2026-10-25` (MCM race date). Tests that assert "today's plan is week 4" will break if a fixture shifts. **Feat C must include test isolation**: any new `test_plan_start_date.py` test that calls the shift endpoint must work on a per-test athlete/plan, not a shared fixture, to avoid cross-test pollution.
- **Feat D has zero backend test impact.**

### 2f. State / status enum conflicts

The `WorkoutStatus` enum is touched implicitly:

- Feat A §4 step 2 reads `status in (done, skipped)` for its 409 guard. **Stable** unless `abandoned` is added.
- Feat B's adherence math (Feat B §4 → `_last_n_days_adherence`) counts `status ∈ {done, moved}`. **Stable** unless `abandoned` is added — and if it is, Feat B should clarify whether `abandoned` counts as "off plan" (probably yes) which is the `adh_total - adh_done` denominator's natural behavior.
- Feat C §5 step 2 marks abandoned-past-planned rows as `skipped` **today**, but open Q2 asks whether to introduce a new `abandoned` value.
- Feat D §4 doesn't switch on status anywhere directly, but `WorkoutCard.tsx:48` (the type pill) and the cycle-lane `WeekTile.tsx` (Feat D §4.6) render differently per status today. A new `abandoned` status would need a color/copy decision — Feat D didn't budget for it.

**This is the most cross-cutting open question. If `abandoned` lands, all four features touch it.** See the recommended phase plan (§4) for ordering.

---

## 3. Dependency graph

```
                    +---------------------------+
                    | Phase 1 — Foundation       |
                    | (must precede all UI work) |
                    +---------------------------+
                       |          |         |
      +----------------+          |         +-----------------+
      |                           |                           |
      v                           v                           v
+-----------+         +---------------------+         +---------------+
| D tokens  |         | invalidate_for_     |         | A migration:  |
| + fonts   |         | athlete umbrella    |         | nullable      |
| (no deps) |         | (B owns; A+C use)   |         | garmin_id     |
+-----------+         +---------------------+         +---------------+
                                                              |
                                                              |  parallel
                                                              v
                                                      +---------------+
                                                      | C migration:  |
                                                      | plan_history  |
                                                      +---------------+

      +----- Phase 2 — backend endpoints (parallel) -------+
      |                       |                            |
      v                       v                            v
+-----------+         +-----------------+         +-----------------+
| A: POST   |         | B: GET recent + |         | C: POST plan/   |
| log-      |         | coach_brief     |         | start-date      |
| completed |         | (replaces       |         | (with dry_run)  |
+-----------+         | test_plan_      |         +-----------------+
                      | routes:20 fixture)
                      +-----------------+

      +----- Phase 3 — mobile (D-blocked) ------------------+
      |                       |                             |
      v                       v                             v
+-----------------------+   +-----------------------+   +--------------------+
| D: typography sweep   |   | A: LogCompletedSheet  |   | C: StartDateSheet  |
| across components     |   | + useLogCompleted     |   | + Settings/Program |
| (10 PR rows per §8.4) |   | + WorkoutDetail wire  |   | + ProgramSettings  |
+-----------------------+   +-----------------------+   +--------------------+
                                       |
                                       v (TodayScreen consolidation)
                              +-------------------------+
                              | TodayScreen consolidated |
                              | edit:                    |
                              |   - D typography         |
                              |   - A SYNC pill          |
                              |   - B coach brief card   |
                              |   - B RecentRunsStrip    |
                              +-------------------------+
```

**Key constraints:**

- **D before A's sheet, B's strip, C's sheet.** Otherwise we ship retrofit PRs.
- **`invalidate_for_athlete` umbrella before A's and C's mutation handlers.** Otherwise A and C call `invalidate_plan_cache` and we ship cache-stale bugs.
- **Feat A's nullable migration before Feat B's mobile hook regenerates types.** Otherwise the generated `CompletedWorkoutOut.garmin_activity_id` doesn't allow `null` and Feat A's response fails client validation. (Both can be authored in parallel; just sequence the type regen.)
- **Feat C's `plan_history` migration is fully independent.**
- **TodayScreen must be a single PR** (or two — D first, then A+B together) to avoid merge conflicts on a hot file.

---

## 4. Recommended phase plan for the planner

### Phase 1 — Foundation (parallel; ~1 day)

Before any feature endpoint or UI ships:

1. **Cache umbrella PR** (Feat B owns the design; Phase 1 ships only the helper, not the recent-completed or coach-brief implementations).
   - New `app/services/cache.py` with `invalidate_for_athlete(athlete_id)` that fans out to `invalidate_plan_cache`, `invalidate_recent_completed_cache` (stub if Feat B not yet built), and `invalidate_coach_brief_cache` (stub).
   - Update existing call sites: `garmin_sync.py:239-266`, `app/routes/workouts.py` (skip / edit / move handlers) to call the umbrella.
   - Ships green with **no behavior change** (the stubs are no-ops until B fills them).

2. **Feat A migration** — alembic revision making `completed_workouts.garmin_activity_id` nullable. Run `alembic upgrade head` in-Docker. Models + schema field type updates (`Mapped[int | None]`). Re-run `scripts/export_openapi.py` so the mobile type updates.

3. **Feat C migration** — alembic revision creating `plan_history`. New `app/models/plan_history.py`. No route yet.

4. **Feat D foundation PR** — `mobile/assets/fonts/JetBrainsMono-Regular.ttf` + `JetBrainsMono-Bold.ttf` dropped in. `App.tsx` `useFonts` updated. `mobile/src/theme/tokens.ts` adds `fonts.mono` and `fonts.monoBold`. **Zero component edits in this PR** — just registers the families. Ships green; no visible UI change yet.

5. **DECISION GATE — `abandoned` status (Feat C open Q2).** If user says yes: a 6th item in Phase 1 — alembic migration adding the enum value, plus a `WorkoutStatus.abandoned` reference everywhere `skipped` is enumerated. If no: Feat C uses `skipped` and we move on.

### Phase 2 — Backend endpoints (parallel; ~1.5 days)

All three can ship in parallel because they hit different routes, different tables, and all use the umbrella from Phase 1.

6. **Feat A `POST /workouts/{id}/log-completed`** in `app/routes/workouts.py`. Schemas in `app/schemas/workout.py`. Helper in `app/lib/activity_type.py`. Tests in `tests/routes/test_workouts.py` (8 cases per Feat A §8). Calls `invalidate_for_athlete`.

7. **Feat B `GET /workouts/completed/recent`** in `app/routes/workouts.py` — registered **above** the `{id}` route. New `app/services/coach_brief.py`. `plan_today` calls `compose_today_brief`. Updates `tests/test_plan_routes.py:20` to assert against fixtures that exercise both branches. New `tests/test_recent_completed.py` and `tests/test_coach_brief.py`. Wires the recent-completed and coach-brief invalidators into the Phase 1 umbrella.

8. **Feat C `POST /plan/start-date` (with `?dry_run=true`)** in `app/routes/plan.py`. New `app/services/plan_shift.py`. New `app/lib/cycle_constants.py` with `MIN_WEEKS_TO_RACE`. Tests in `tests/routes/test_plan_start_date.py` (10 cases per Feat C §8). Calls `invalidate_for_athlete`.

   **Resolve the URL question:** use `?dry_run=true` query param (Feat C §5), drop the `/preview` sibling endpoint mentioned in Feat C §6.

### Phase 3 — Mobile typography sweep (Feat D, ~2 days, can interleave with Phase 2)

9. **Feat D component sweep** following Feat D §8.4's 10-step order. Each sub-step is an independent small PR:
   1. `RetroButton` per-tone family map.
   2. `WorkoutCard` (title to monoBold 20, type to RetroPill badge, metas to mono 13, mini-buttons to mono 11).
   3. `WhySheet`.
   4. `WorkoutDetailScreen` (title, comparison heads, section labels — but **not** the new `MARK DONE` button row, that ships with Feat A).
   5. `EditQuestSheet`.
   6. `ProposalSheet`.
   7. `DisplacedSheet`.
   8. `DayCard` + `DraggableWeekList`.
   9. `StatTile`.
   10. Page titles on all screens **except `TodayScreen`** (consolidated in Phase 4) — Login, Week, Program, Settings.
   11. `RootNavigator` tab font 8 → 9.

### Phase 4 — Mobile feature wire-up (depends on Phase 2 + Phase 3; ~1.5 days)

10. **Feat A mobile** — `LogCompletedSheet.tsx` (uses Feat D tokens from day one). `useLogCompleted.ts`, `useSync.ts`. `WorkoutDetailScreen` MARK DONE button row.

11. **Feat C mobile** — `StartDateSheet.tsx`, `ProgramSettingsBlock.tsx` (or inline), `useResetStartDateMutation`, `usePreviewStartDate`. `SettingsScreen` Program section.

12. **TodayScreen consolidated PR** — single PR that:
    - Applies Feat D's typography changes (page title monoBold 20, header line mono 12, REST DAY label mono 12).
    - Adds Feat A's header `SYNC` pill + `useSync` wiring.
    - Replaces Feat B's coach-brief and recent-runs placeholders with `RecentRunsStrip` + conditional brief card.

   Sequenced **last** in the sprint to avoid hot-file conflicts.

### Phase 5 — Sprint close

13. Re-run `scripts/export_openapi.py`; verify `mobile/src/api/types.ts` is consistent.
14. End-to-end smoke test in-Docker per CLAUDE.md "Testing Protocol": log a workout, sync, shift the start date, verify the brief reads sensibly.
15. `ruff check && ruff format`; mobile `tsc --noEmit`; `pytest`. All clean.

---

## 5. Open questions consolidated

### BLOCKING (user must answer before code starts)

| # | Question | Source | Why blocking |
|---|---|---|---|
| 1 | Add a new `abandoned` `WorkoutStatus` value, or reuse `skipped`? | Feat C open Q2 | Schema migration; touches Feat A guards (§4 step 2), Feat B adherence math (§4), Feat D status-pill rendering. Different code path either way. |
| 2 | Confirm `MIN_WEEKS_TO_RACE` (16 / 8 / 9) for MCM / Disney / Delaware. | Feat C open Q5 | Hard 400 boundary in Feat C's validation. Wrong values = user can't shift the plan even when they should be able to. |
| 3 | Token names — `mono` / `monoBold` or `body` / `bodyBold`? | Feat D open Q2 | Foundational; renamed after adoption is ~30 file edits. Decide at Phase 1. |

### SOFT-BLOCKING (user input wanted, but engineer can default and iterate)

| # | Question | Source | Default if user silent |
|---|---|---|---|
| 4 | "MARK DONE" copy or "LOG QUEST" or "MARK COMPLETE"? | Feat A open Q4 | "Log workout" mixed-case under Feat D mono CTA tone. |
| 5 | Pace input format on `LogCompletedSheet` — confirm min/mile (US) not min/km. | Feat A open Q1 | min/mile (US default per existing PaceTargets surface). |
| 6 | Does TodayScreen `SYNC` pill auto-fire on focus? | Feat A open Q3 | No (cost / rate-limit risk). |
| 7 | Coach-brief tone — declarative vs directive? | Feat B open Q3 | Declarative for v1; settings toggle deferred. |
| 8 | Multiple workouts on "today" — list both or pick one? | Feat B open Q5 | Pick first; v1.1 follow-up to join titles. |
| 9 | Pace source for recent-runs strip — derive client-side or backfill server? | Feat B open Q2 | Client-side (a) for v1; tracker for backfill. |
| 10 | Shift-date: edits travel with workout, or stick to calendar? | Feat C open Q1 | Travel with workout (matches user mental model). |
| 11 | Day-of-week alignment — prior-Monday or nearest-Monday? | Feat C open Q6 | Prior-Monday (avoids forward-snap past intent). |
| 12 | `peak_week_target` recompute on shift? | Feat C open Q3 | Preserve in v1; recompute only in reseed mode. |
| 13 | Tap target on recent-run mini-card — sheet or detail? | Feat B open Q1 | Sheet (per spec recommendation). |
| 14 | Strength activities in the recent-runs strip? | Feat B open Q4 | Show with family swatch; defer filter pill. |
| 15 | Drop `.toUpperCase()` on quick-pick chips? | Feat D open Q3 | Keep caps (chips are badges in spirit). |
| 16 | Move `SectionHeader` from VT323 to mono? | Feat D open Q1 | Leave VT323 in this pass (matches Session 2.6). |
| 17 | When does VT323 retire? | Feat D open Q4 | Not this pass. |
| 18 | `started_at` precision for manual logs (midnight vs nothing). | Feat A open Q2 | Midnight (already specced). |
| 19 | Reconciliations to `status='moved'` rows after shift — verify UX. | Feat C open Q4 | Tested in Phase 2. |
| 20 | Reseed mode timeline. | Feat C open Q7 | v1.1, return 501 in v1. |

---

## 6. Risk register

| # | Risk | Likelihood | Mitigation |
|---|---|---|---|
| 1 | **Cache fan-out lands after A/B/C, not before.** A's manual log + B's strip + C's shift each call only `invalidate_plan_cache` and stale data shows for 60s. | Medium — easy to skip the umbrella under sprint pressure. | Mandate Phase 1 ships the umbrella *first*, with stub fan-outs. Reject any A/C PR that calls `invalidate_plan_cache` directly in code review. |
| 2 | **JetBrains Mono metrics differ from PressStart2P, breaking layouts.** PressStart2P is a fixed 8×8 pixel font with no descenders; JetBrains Mono Regular at 11pt has different x-height and ascender/descender extents. RetroPill bracket alignment, button vertical centering, and StatTile `minHeight: 76` (Feat D §5 bumps to 80) all assume current metrics. | High — visual regressions are the #1 risk on a typography swap. | Smoke pass screen-by-screen per Feat D §9 step 10. Make `StatTile.minHeight` and any `lineHeight` overrides explicit, not implicit. Test on both iOS and Android per Feat D §3.2 note. |
| 3 | **`abandoned` status decision deferred → mid-sprint reroll.** If user decides on `abandoned` *after* Feat C's PR is up, the migration cascades into Feat A, B, D — all of which are presumed locked. | Medium — depends on user availability. | Force the decision at Phase 1 gate. If user is unreachable, the engineer picks `skipped` (matches Feat C's spec default at §5 step 2) and notes it in the PR description. |
| 4 | **TodayScreen merge conflict.** Three features touch the same screen file; if A's mobile PR and B's mobile PR both branch off main without sequencing, the second-merged PR conflicts. | High — almost guaranteed without the consolidated-PR pattern. | Phase 4 step 12 explicitly consolidates TodayScreen into a single PR, sequenced last after A's and B's *non-TodayScreen* mobile work has merged. |
| 5 | **Coach-brief test fixture rework drags on.** `tests/test_plan_routes.py:20` is one assertion today (Feat B §8); fixing it well requires fixture athletes for "rest day", "easy day", "hard day", "no plan" — the fixture sprawl can grow. | Medium — Feat B §10 references this but doesn't budget time. | Treat fixture rework as part of Feat B's PR scope, not a follow-up. Engineer should pattern-match existing fixture style in `tests/conftest.py` (or wherever the seed athlete lives). |
| 6 | **OpenAPI regen ordering.** If Feat B's mobile hook is written before Feat A's nullable migration ships, the generated `CompletedWorkoutOut.garmin_activity_id` is still `number` (not `number | null`), and Feat A's response trips client-side validation when it lands. | Low — but silent and confusing if it happens. | Phase 1 explicitly bundles Feat A's migration with `scripts/export_openapi.py` rerun. Feat B mobile hook can't begin until that runs. Document in Phase 2 step 7 of the plan. |

---

## 7. Files referenced (absolute paths)

- `C:/Coding Projects/marathon_app/docs/superpowers/specs/2026-05-07-feat-a-mark-complete-dialog-design.md`
- `C:/Coding Projects/marathon_app/docs/superpowers/specs/2026-05-07-feat-b-recent-runs-and-coach-brief-design.md`
- `C:/Coding Projects/marathon_app/docs/superpowers/specs/2026-05-07-feat-c-program-start-date-design.md`
- `C:/Coding Projects/marathon_app/docs/superpowers/specs/2026-05-07-feat-d-visual-crispness-design.md`
- `C:/Coding Projects/marathon_app/docs/superpowers/specs/2026-05-07-session-2.7-cross-cutting-review.md` (this doc)
