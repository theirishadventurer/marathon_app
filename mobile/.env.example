# Set to the LAN IP of your dev machine running `docker compose up`
# (localhost won't work from a real iPhone via Expo Go).
# e.g. EXPO_PUBLIC_API_URL=http://192.168.1.42:8000
EXPO_PUBLIC_API_URL=http://localhost:8000
